# ===================================================================
# H?BR?T YAPILANDIRMA (LOCAL + PRODUCTION)
# Format: ${ORTAM_DEGISKENI:Varsay?lan_Local_Deger}
# ===================================================================

# 1. Veritaban? Ba?lant?s?
# Render: 'DB_URL' ortam de?i?kenini kullan?r.
# Local: Bulamazsa 'jdbc:postgresql://localhost:5432/postgres' kullan?r.
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/postgres}

# Render: 'DB_USER' ortam de?i?kenini kullan?r.
# Local: 'stilrehberi_kisisel_user' kullan?r.
spring.datasource.username=${DB_USER:stilrehberi_kisisel_user}  

# Render: 'DB_PASSWORD' ortam de?i?kenini kullan?r.
# Local: Sizin yerel ?ifreniz (554433dunya) kullan?l?r.
spring.datasource.password=${DB_PASSWORD:554433dunya}

# 2. JPA & Hibernate Ayarlar?
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# NOT: Render'da bu de?i?keni silmi?tik, bu yüzden varsay?lan public ?emas? kullan?l?r.
# Localde de public ?emas?yla çal??mas? en sa?l?kl?s?d?r.
# E?er yerel veritaban?n?zda 'stilrehberi' ?emas? zorunluysa a?a??daki sat?r? açabilirsiniz:
# spring.jpa.properties.hibernate.default_schema=stilrehberi

# 3. JWT (Güvenlik) Ayarlar?
# Render: 'JWT_SECRET_KEY' de?i?kenini kullan?r.
# Local: Varsay?lan uzun anahtar? kullan?r.
application.security.jwt.secret-key=${JWT_SECRET_KEY:bW9kYXRhdnNpeWVsZXJpcHJvamVzaWljaW5ndXZlbmxpa2FuYWh0YXJpZGly}
application.security.jwt.expiration=86400000

# 4. Swagger UI (Dokümantasyon)
springdoc.swagger-ui.path=/swagger-ui.html

# 5. DataSeeder (?lk Admin Olu?turma)
# Render: Environment de?i?kenlerini kullan?r.
# Local: Varsay?lan de?erleri kullan?r.
admin.default.username=${ADMIN_DEFAULT_USERNAME:admin}
admin.default.email=${ADMIN_DEFAULT_EMAIL:admin@stilrehberi.com}
admin.default.password=${ADMIN_DEFAULT_PASSWORD:AdminSifresi123!}