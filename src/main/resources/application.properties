# ===================================================================
# HIBRIT YAPILANDIRMA (LOCAL + PRODUCTION)
# Format: ${ORTAM_DEGISKENI:Varsayilan_Local_Deger}
# ===================================================================

# 1. Veritabani Baglantisi
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/postgres}
spring.datasource.username=${DB_USER:stilrehberi_kisisel_user}
spring.datasource.password=${DB_PASSWORD:554433dunya}

# 2. JPA & Hibernate Ayarlari
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# 3. JWT (Guvenlik) Ayarlari
# Secret key 256-bit olmalidir.
application.security.jwt.secret-key=${JWT_SECRET_KEY:bW9kYXRhdnNpeWVsZXJpcHJvamVzaWljaW5ndXZlbmxpa2FuYWh0YXJpZGly}
application.security.jwt.expiration=86400000
# EKS?K OLAN KISIM BURASIYDI (Genellikle gereklidir):
application.security.jwt.refresh-token.expiration=604800000

# 4. Swagger UI (Dokumantasyon)
springdoc.swagger-ui.path=/swagger-ui.html

# 5. DataSeeder (Ilk Admin Olusturma)
admin.default.username=${ADMIN_DEFAULT_USERNAME:admin}
admin.default.email=${ADMIN_DEFAULT_EMAIL:admin@stilrehberi.com}
# Sondaki yazi hatasi temizlendi:
admin.default.password=${ADMIN_DEFAULT_PASSWORD:AdminSifresi123!}